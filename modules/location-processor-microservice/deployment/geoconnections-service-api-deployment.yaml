apiVersion: v1
kind: Service
metadata:
  labels:
    service: location-processor-api
  name: location-processor-api
spec:
  ports:
  - name: "5000"
    port: 5000
    targetPort: 5000
    nodePort: 30003
  selector:
    service: location-processor-api
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: location-processor-api
  name: location-processor-api
spec:
  replicas: 1
  selector:
    matchLabels:
      service: location-event-api
  template:
    metadata:
      labels:
        service: location-event-api
    spec:
      containers:
      - image: fthiagomedeiros/location-processor-service:latest
        name: location-processor-container
        imagePullPolicy: Always
        env:
        - name: KAFKA_URL
          valueFrom:
            configMapKeyRef:
              name: config-location-processor-map
              key: KAFKA_URL
        - name: KAFKA_TOPIC
          valueFrom:
            configMapKeyRef:
              name: config-location-processor-map
              key: KAFKA_TOPIC
      restartPolicy: Always
